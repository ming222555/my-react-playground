import React from 'react';

import './HomePageHeader.scss';

function HomePageHeader() {
    console.log('function HomePageHeader');
    return (
        <nav className="homePageHeader">
            HomePageHeader
        </nav>
    );
}

export default React.memo( HomePageHeader);
